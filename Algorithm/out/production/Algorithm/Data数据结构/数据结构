### 数据结构
##### 线性结构

双向列表 vs 单向列表  删除操作时间复杂度降低一半

双向链表 vs 动态数组

动态数组：开辟销毁空间次数少，但是造成内存浪费（可以缩容解决）。查询快
双向链表：开辟销毁内存空间次数多，但是不浪费空间。操作快

栈 vs 队列


栈 可以用动态数组或者链表实现



|最好最差平均|动态数组|链表|
| ---- | ---- | ---- |
|add|O(1),O(n),O(n)|O(1),O(n),O(n)|
|remove|O(1),O(n),O(n)|O(1),O(n),O(n)|
|get|O(1),O(1),O(1)|O(1),O(n),O(n)|
|set|O(1),O(1),O(1)|O(1),O(n),O(n)|

##### 树形结构
##### 图形结构
